export enum COMMON_ROUTES {
    HOME = "/",
}

export enum AUTH_ROUTES {
    LOGIN = "/login",
    REGISTER = "/register",
}

export enum USER_ROUTES {
    PROJECTS = "/projects",
    PROJECT = "/projects/:id",
}
